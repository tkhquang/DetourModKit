name: Architecture Gate

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review, converted_to_draft]
    # This context is intended to be required by the repository ruleset. Keep the trigger unconditional so every
    # pull request reports the required context, including changes outside the architecture-sensitive paths.
    # Draft pull requests skip every job. A skipped context satisfies the ruleset, a draft cannot merge, and
    # ready_for_review re-runs the workflow before a merge is possible.
  workflow_dispatch:
    inputs:
      expected_sha:
        description: "Exact 40-character commit SHA to gate"
        required: true
        type: string

# Read-only: the gate compiles headers with -fsyntax-only and never writes to the repository.
permissions:
  contents: read

# One live run per pull request: a new push supersedes the previous run, and the previous run is canceled. Dispatch
# evidence runs are never canceled. Their group falls back to the ref, and cancellation applies only to pull_request
# events.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  # Reuse the exact pinned GCC the build/coverage gate and the released artifact ship with. The
  # gate's #error semantics are compiler-version-independent, but sharing this cache key with
  # pr-check.yml means the MinGW toolchain is downloaded at most once across the two workflows.
  MINGW_BIN: C:\ProgramData\mingw64\mingw64\bin

jobs:
  arch-gate:
    name: x86-64 architecture gate (blocking)
    if: ${{ !github.event.pull_request.draft }}
    runs-on: windows-latest
    # A stalled Chocolatey install or a hung compile must not block a PR on this required gate. The real
    # work is a cold MinGW install plus two -fsyntax-only header compiles, so 15 minutes is generous
    # headroom that still fails fast on a hang.
    timeout-minutes: 15
    # This gate is not advisory. DetourModKit supports only x86-64 (Win64). Address stores a 64-bit pointer.
    # The scan and hook engines assume the x86-64 register file and instruction encodings.
    # This job rejects a non-x64 target that bypasses the defines.hpp #error.
    # It also rejects an engine static_assert cascade that hides the clear error.
    # The job has no continue-on-error, like the header-encapsulation gate in quality.yml.
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # No submodules: the probe compiles only public headers, which the header-encapsulation
          # gate already keeps free of any external/ backend include, so nothing under external/ is
          # reached by -fsyntax-only. Skipping the submodule fetch keeps this gate fast.
          persist-credentials: false

      - name: Assert the manual dispatch resolved to the exact candidate
        # A pull-request run is bound to its own head by the event. A manual run is not, so evidence recorded at a
        # final candidate has to say which commit it describes; the same helper decides it for every dispatchable
        # required context.
        env:
          EXPECTED_SHA: ${{ inputs.expected_sha }}
          EVENT_SHA: ${{ github.sha }}
        if: ${{ github.event_name == 'workflow_dispatch' }}
        run: python scripts/check_release_identity.py --expected-sha "$EXPECTED_SHA" --event-sha "$EVENT_SHA" --verify-checkout
        shell: bash

      - name: Cache MinGW
        id: cache-mingw
        # Third-party actions are pinned to a full commit SHA (like the checkout above), not a moving
        # tag, so a compromised or retagged release cannot alter this gate. The comment tracks the human
        # version the SHA corresponds to.
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: C:\ProgramData\mingw64
          key: ${{ runner.os }}-mingw-14.2.0-v1

      - name: Install MinGW (if not cached)
        if: steps.cache-mingw.outputs.cache-hit != 'true'
        run: choco install mingw --version=14.2.0 --yes --force --no-progress
        shell: powershell

      - name: Add MinGW to PATH
        run: echo "${{ env.MINGW_BIN }}" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
        shell: powershell

      - name: Run x86-64 architecture-gate check
        # Clear the compiler's __x86_64__ predefine to simulate a non-x86-64 target.
        # Require the clear defines.hpp #error instead of an opaque engine static_assert cascade.
        # Also require a clean native x64 syntax check of the same headers.
        run: bash scripts/check_arch_gate.sh
        shell: bash
